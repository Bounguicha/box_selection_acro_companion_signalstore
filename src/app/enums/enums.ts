/**
 * Enum to define different button categories.
 * Used to distinguish between categories of button actions.
 */
export enum BUTTON_CATEGORIES {
  NUMBER = "Number",
  CHARACTER = "Character",
  SYMBOL = "Symbol",
}
